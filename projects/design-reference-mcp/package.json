{
  "name": "design-reference-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server for design reference research: search galleries, repos, design systems, extract tokens, and grow a self-indexed library of keepers.",
  "type": "module",
  "bin": {
    "design-reference-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/sources && cp src/sources/registry.json dist/sources/registry.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "cli": "tsx src/index.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "playwright": "^1.48.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
